/** Paywall URL helpers — aligned with @circle-plugins/plugin-eliza params. */
export declare function defaultPaywallUrl(): string;
/** Ensure paywall requests always hit the /risk-profile route when appropriate. */
export declare function normalizePaywallUrl(url: string): string;
export declare function resolvePaywallUrl(url?: string): string;
//# sourceMappingURL=params.d.ts.map